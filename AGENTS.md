# Project Instructions

- Repository target: `DexterMiller/CallRoutingDiagramTool`.
- Default pull request base branch: `main`.
- After making code or configuration changes for this project, commit the relevant changes to a `codex/` branch, push it, and open a pull request against `DexterMiller/CallRoutingDiagramTool:main` unless the user explicitly asks not to.
